# board allows for input
# holds board
# displays board
# checks to see if move is legal

def move(start,end):
	#moves piece from starting point to ending point
	if islegal(start,end) == true:
		#move piece
		#return true
	else:
		#tell player move is invalid
		#return false

def islegal(start,end):
	#check rules and positions to see if move is legal


def playgame():
	while move(XXX,YYY) == False:
		